# Default values for ambassador-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  name: ambassador-operator:dev
  pullPolicy: IfNotPresent

namespace: ambassador